{
  "name": "honknet-web",
  "private": true,
  "version": "1.0.0-rc.1",
  "type": "module",
  "scripts": {
    "build:wasm": "cargo build --target wasm32-unknown-unknown --release -p honknet-web && wasm-bindgen ../../target/wasm32-unknown-unknown/release/honknet_web.wasm --out-dir pkg --target web",
    "dev": "vite --host 0.0.0.0",
    "build": "npm run build:wasm && tsc && vite build",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "pixi.js": "^8.0.0"
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "vite": "^6.0.0"
  }
}
