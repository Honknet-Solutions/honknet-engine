server { listen 443 ssl http2; server_name game.example.com; root /srv/honknet-web; location / { try_files $uri /index.html; } location /ws { proxy_pass http://127.0.0.1:3016; proxy_http_version 1.1; proxy_set_header Upgrade $http_upgrade; proxy_set_header Connection "upgrade"; } }
