id: example-door
events:
  OnInteract:
    - node: Branch
      condition:
        node: HasComponent
        entity: $self
        component: Door
      success:
        - node: ToggleDoor
          entity: $self
      failure:
        - node: Log
          level: warn
          message: Entity does not have Door component
