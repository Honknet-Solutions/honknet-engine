{
  "name": "@honknet/studio",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node dev.mjs",
    "build": "tsc && vite build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --pool=forks --maxWorkers=1 --minWorkers=1 --no-file-parallelism",
    "test:watch": "vitest",
    "dev:vite": "vite --host 0.0.0.0"
  },
  "dependencies": {
    "@honknet/hui-runtime": "file:../../packages/hui-runtime",
    "yaml": "^2.7.0"
  },
  "devDependencies": {
    "happy-dom": "^18.0.1",
    "typescript": "^5.8.0",
    "vite": "^6.4.0",
    "vitest": "^3.2.7",
    "ws": "^8.21.1"
  }
}
